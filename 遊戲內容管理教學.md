# 🎮 GachaEgg 遊戲內容管理教學

本文件教您如何自訂遊戲內容：修改扭蛋資料、調整機率、添加圖片等。

---

## 📋 目錄

1. [修改扭蛋類型](#修改扭蛋類型)
2. [修改獎品池與機率](#修改獎品池與機率)
3. [添加圖片資源](#添加圖片資源)
4. [修改冷卻時間](#修改冷卻時間)
5. [部署更新](#部署更新)

---

## 🎰 修改扭蛋類型

扭蛋類型定義在 `server/gacha/eggs.json` 文件中。

### 檔案位置
```
server/gacha/eggs.json
```

### 當前內容

```json
[
  {
    "id": "egg30",
    "price": 30,
    "pool": "pool30",
    "name": "30元扭蛋"
  },
  {
    "id": "egg50",
    "price": 50,
    "pool": "pool50",
    "name": "50元扭蛋"
  },
  {
    "id": "egg100",
    "price": 100,
    "pool": "pool100",
    "name": "100元扭蛋"
  }
]
```

### 欄位說明

| 欄位 | 說明 | 範例 |
|------|------|------|
| `id` | 唯一識別碼（英文） | `"egg30"` |
| `price` | 扭蛋價格（數字） | `30` |
| `pool` | 對應的獎池 ID | `"pool30"` |
| `name` | 顯示名稱（中文） | `"30元扭蛋"` |

### 範例：新增一個 200 元扭蛋

```json
[
  {
    "id": "egg30",
    "price": 30,
    "pool": "pool30",
    "name": "30元扭蛋"
  },
  {
    "id": "egg50",
    "price": 50,
    "pool": "pool50",
    "name": "50元扭蛋"
  },
  {
    "id": "egg100",
    "price": 100,
    "pool": "pool100",
    "name": "100元扭蛋"
  },
  {
    "id": "egg200",
    "price": 200,
    "pool": "pool200",
    "name": "200元豪華扭蛋"
  }
]
```

**注意：** 新增扭蛋後，需要在 `pools.json` 中添加對應的獎池 `pool200`

---

## 🎁 修改獎品池與機率

獎品池定義在 `server/gacha/pools.json` 文件中。

### 檔案位置
```
server/gacha/pools.json
```

### 當前內容（範例：pool30）

```json
{
  "pool30": [
    {
      "name": "小魚乾",
      "prob": 0.50,
      "price": 10,
      "image": "/items/fish.png",
      "isJackpot": false
    },
    {
      "name": "貓薄荷",
      "prob": 0.30,
      "price": 25,
      "image": "/items/catnip.png",
      "isJackpot": false
    },
    {
      "name": "貓罐頭",
      "prob": 0.15,
      "price": 50,
      "image": "/items/can.png",
      "isJackpot": false
    },
    {
      "name": "黃金貓罐（大獎）",
      "prob": 0.05,
      "price": 300,
      "image": "/items/gold.png",
      "isJackpot": true
    }
  ]
}
```

### 欄位說明

| 欄位 | 說明 | 範例 | 注意事項 |
|------|------|------|----------|
| `name` | 獎品名稱 | `"小魚乾"` | 顯示給玩家 |
| `prob` | 中獎機率（0-1） | `0.50` | 所有機率加總必須為 1.0 |
| `price` | 獎品價值 | `10` | 用於計算對戰勝負 |
| `image` | 圖片路徑 | `"/items/fish.png"` | 相對於 `server/public/` |
| `isJackpot` | 是否為大獎 | `true` 或 `false` | true 會觸發全服廣播 |

### 機率計算說明

所有 `prob` 值的總和**必須等於 1.0**（或 100%）

**範例：**
```
小魚乾: 0.50 (50%)
貓薄荷: 0.30 (30%)
貓罐頭: 0.15 (15%)
黃金貓罐: 0.05 (5%)
總計: 1.00 (100%) ✅
```

### 範例 1：調整機率（降低大獎機率）

```json
{
  "name": "黃金貓罐（大獎）",
  "prob": 0.01,
  "price": 300,
  "image": "/items/gold.png",
  "isJackpot": true
}
```

**記得調整其他項目的機率，確保總和為 1.0！**

### 範例 2：新增獎品

```json
"pool30": [
  {
    "name": "小魚乾",
    "prob": 0.45,
    "price": 10,
    "image": "/items/fish.png",
    "isJackpot": false
  },
  {
    "name": "貓薄荷",
    "prob": 0.25,
    "price": 25,
    "image": "/items/catnip.png",
    "isJackpot": false
  },
  {
    "name": "貓罐頭",
    "prob": 0.15,
    "price": 50,
    "image": "/items/can.png",
    "isJackpot": false
  },
  {
    "name": "貓玩具",
    "prob": 0.10,
    "price": 40,
    "image": "/items/toy.png",
    "isJackpot": false
  },
  {
    "name": "黃金貓罐（大獎）",
    "prob": 0.05,
    "price": 300,
    "image": "/items/gold.png",
    "isJackpot": true
  }
]
```

**檢查：** 0.45 + 0.25 + 0.15 + 0.10 + 0.05 = 1.00 ✅

### 範例 3：建立新的獎池（pool200）

如果您新增了 200 元扭蛋，需要建立對應的獎池：

```json
{
  "pool30": [...],
  "pool50": [...],
  "pool100": [...],
  "pool200": [
    {
      "name": "高級飼料",
      "prob": 0.40,
      "price": 150,
      "image": "/items/premium-food.png",
      "isJackpot": false
    },
    {
      "name": "貓跳台",
      "prob": 0.35,
      "price": 200,
      "image": "/items/cat-tower.png",
      "isJackpot": false
    },
    {
      "name": "智能貓砂盆",
      "prob": 0.15,
      "price": 350,
      "image": "/items/smart-litter.png",
      "isJackpot": false
    },
    {
      "name": "鑽石項圈（超級大獎）",
      "prob": 0.10,
      "price": 1000,
      "image": "/items/diamond-collar.png",
      "isJackpot": true
    }
  ]
}
```

---

## 🖼️ 添加圖片資源

### 圖片存放位置

```
server/public/items/
```

### 支援的圖片格式
- PNG（推薦，支援透明背景）
- JPG / JPEG
- GIF
- WebP

### 建議尺寸
- **寬度**: 200-400 像素
- **高度**: 200-400 像素
- **比例**: 1:1（正方形）
- **檔案大小**: < 100KB

### 添加圖片步驟

#### 1. 準備圖片文件

將圖片命名為有意義的名稱（使用英文）：
- ✅ `fish.png`
- ✅ `gold-can.png`
- ✅ `cat-tower.png`
- ❌ `圖片1.png`
- ❌ `IMG_0001.png`

#### 2. 放置圖片

將圖片文件放入 `server/public/items/` 目錄：

```
server/
└── public/
    └── items/
        ├── fish.png
        ├── catnip.png
        ├── can.png
        ├── gold.png
        └── toy.png  ← 新增的圖片
```

#### 3. 在 pools.json 中引用

```json
{
  "name": "貓玩具",
  "prob": 0.10,
  "price": 40,
  "image": "/items/toy.png",
  "isJackpot": false
}
```

**重要：** 路徑必須以 `/items/` 開頭

### 圖片優化建議

**使用線上工具壓縮圖片：**
- TinyPNG: https://tinypng.com/
- Squoosh: https://squoosh.app/

**好處：**
- 加快載入速度
- 節省流量
- 提升使用者體驗

### 沒有圖片怎麼辦？

如果沒有準備圖片，系統會顯示預設的 🎁 圖示：

```json
{
  "name": "神秘獎品",
  "prob": 0.10,
  "price": 50,
  "image": "",
  "isJackpot": false
}
```

**建議：** 至少為大獎準備專屬圖片，提升視覺效果

---

## ⏰ 修改冷卻時間

冷卻機制定義在 `server/socket/handlers/singlePlayHandler.js`

### 檔案位置
```
server/socket/handlers/singlePlayHandler.js
```

### 當前設定

```javascript
const DRAW_LIMIT = 10; // 抽卡上限
const COOLDOWN_TIME = 5000; // CD 時間（毫秒）
```

### 修改冷卻參數

#### 修改抽卡上限（從 10 次改為 20 次）

```javascript
const DRAW_LIMIT = 20; // 抽卡上限
```

#### 修改冷卻時間（從 5 秒改為 10 秒）

```javascript
const COOLDOWN_TIME = 10000; // CD 時間（毫秒）
```

**時間換算：**
- 1 秒 = 1000 毫秒
- 5 秒 = 5000 毫秒
- 10 秒 = 10000 毫秒
- 30 秒 = 30000 毫秒
- 1 分鐘 = 60000 毫秒

#### 完全關閉冷卻（不推薦）

將 DRAW_LIMIT 設定為一個很大的數字：

```javascript
const DRAW_LIMIT = 99999; // 實際上無限制
```

**警告：** 關閉冷卻可能導致伺服器負載過高

---

## 🎨 修改 UI 文字

### 修改首頁標題

**檔案位置：** `client/src/pages/Home.jsx`

找到：
```jsx
<h1 className="home-title">🎰 多人抽蛋對戰遊戲</h1>
```

修改為：
```jsx
<h1 className="home-title">🎰 我的扭蛋遊戲</h1>
```

### 修改模式按鈕文字

**檔案位置：** `client/src/pages/Home.jsx`

找到：
```jsx
<div className="mode-title">單人抽蛋</div>
<div className="mode-desc">自由抽蛋，累積紀錄</div>
```

修改為：
```jsx
<div className="mode-title">練習模式</div>
<div className="mode-desc">無限抽蛋，測試手氣</div>
```

### 修改冷卻提示訊息

**檔案位置：** `server/socket/handlers/singlePlayHandler.js`

找到：
```javascript
message: '你抽太多了！請稍後再試'
```

修改為：
```javascript
message: '請休息一下再繼續～'
```

---

## 🚀 部署更新

修改完成後，需要部署到線上環境。

### 步驟 1：本地測試

```bash
# 測試後端
cd server
npm start

# 測試前端（開新終端）
cd client
npm run dev
```

確認所有功能正常運作後再部署。

### 步驟 2：提交變更

```bash
# 回到專案根目錄
cd D:\TestProject\GachaEgg

# 查看修改的文件
git status

# 添加所有變更
git add .

# 提交（用英文描述變更）
git commit -m "Update game content and probabilities"

# 推送到 GitHub
git push origin main
```

### 步驟 3：等待自動部署

**Render（後端）：**
- 自動偵測更新
- 重新建置和部署
- 約 3-5 分鐘完成

**Vercel（前端）：**
- 自動偵測更新
- 重新建置和部署
- 約 1-2 分鐘完成

### 步驟 4：驗證更新

**檢查後端：**
```
https://gatchaegg-backend-xxxx.onrender.com/api/eggs
```

**檢查前端：**
```
https://gatcha-egg-xxxx.vercel.app/
```

按 **Ctrl + F5** 硬重新整理（清除緩存）查看最新版本。

---

## 📝 常見修改範例

### 範例 1：聖誕節主題扭蛋

**修改 eggs.json：**
```json
{
  "id": "xmas50",
  "price": 50,
  "pool": "poolXmas",
  "name": "🎄 聖誕扭蛋"
}
```

**新增 poolXmas 在 pools.json：**
```json
"poolXmas": [
  {
    "name": "聖誕襪",
    "prob": 0.40,
    "price": 30,
    "image": "/items/xmas-sock.png",
    "isJackpot": false
  },
  {
    "name": "薑餅人",
    "prob": 0.35,
    "price": 40,
    "image": "/items/gingerbread.png",
    "isJackpot": false
  },
  {
    "name": "聖誕樹",
    "prob": 0.20,
    "price": 80,
    "image": "/items/xmas-tree.png",
    "isJackpot": false
  },
  {
    "name": "🎅 聖誕老人（大獎）",
    "prob": 0.05,
    "price": 500,
    "image": "/items/santa.png",
    "isJackpot": true
  }
]
```

### 範例 2：提高大獎機率（活動期間）

**修改 pools.json 中的大獎機率：**

原本：
```json
{
  "name": "黃金貓罐（大獎）",
  "prob": 0.05,
  "price": 300,
  "image": "/items/gold.png",
  "isJackpot": true
}
```

活動期間（雙倍機率）：
```json
{
  "name": "黃金貓罐（大獎）",
  "prob": 0.10,
  "price": 300,
  "image": "/items/gold.png",
  "isJackpot": true
}
```

**記得調整其他項目的機率！**

### 範例 3：增加更多扭蛋選項

**在 eggs.json 添加：**
```json
{
  "id": "egg150",
  "price": 150,
  "pool": "pool150",
  "name": "150元進階扭蛋"
},
{
  "id": "egg500",
  "price": 500,
  "pool": "pool500",
  "name": "500元頂級扭蛋"
}
```

**在 pools.json 添加對應的獎池...**

---

## ⚠️ 重要注意事項

### 修改資料時

1. **備份原始文件**
   - 修改前先複製一份備份
   - 以防修改錯誤可以還原

2. **檢查 JSON 格式**
   - 使用 [JSONLint](https://jsonlint.com/) 驗證格式
   - 確保逗號、括號正確
   - 最後一項不要有多餘逗號

3. **機率總和必須為 1.0**
   - 使用計算機確認
   - 誤差不可超過 0.001

4. **圖片路徑區分大小寫**
   - `fish.png` ≠ `Fish.png`
   - 建議全部使用小寫

5. **測試再部署**
   - 本地測試所有功能
   - 確認無誤再推送到線上

### 修改代碼時

1. **逐步修改**
   - 一次修改一個功能
   - 方便定位問題

2. **提交訊息清楚**
   - 用英文描述修改內容
   - 例如：`Update prize pool probabilities`

3. **查看部署日誌**
   - Render 和 Vercel 都提供日誌
   - 出現錯誤時及時查看

---

## 🎨 進階自訂

### 修改顏色主題

**檔案位置：** `client/src/index.css`

找到漸層色設定：
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

修改為您喜歡的顏色：
```css
background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%);
```

**顏色工具：**
- CSS Gradient: https://cssgradient.io/
- Coolors: https://coolors.co/

### 修改字體

**檔案位置：** `client/src/index.css`

```css
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
```

### 添加音效（進階）

需要添加音效庫，例如 Howler.js，並修改相關組件。

建議先學習 React 和 JavaScript 基礎。

---

## 📚 學習資源

### JSON 格式
- JSON 教學：https://www.json.org/json-zh.html
- JSON 驗證工具：https://jsonlint.com/

### 圖片編輯
- Canva（線上設計）：https://www.canva.com/
- Photopea（線上 PS）：https://www.photopea.com/

### Git 基礎
- Git 教學：https://git-scm.com/book/zh-tw/v2
- GitHub 指南：https://guides.github.com/

---

## ✅ 快速檢查清單

修改內容前：
- [ ] 備份原始文件
- [ ] 了解要修改的欄位

修改內容後：
- [ ] 驗證 JSON 格式正確
- [ ] 檢查機率總和為 1.0
- [ ] 本地測試功能正常
- [ ] 提交到 Git
- [ ] 推送到 GitHub
- [ ] 等待自動部署
- [ ] 驗證線上版本

---

## 🎉 完成！

現在您已經學會如何自訂遊戲內容了！

**可以做的事：**
- 🎰 設計專屬的扭蛋主題
- 🎁 調整獎品和機率
- 🖼️ 添加精美圖片
- ⏰ 調整遊戲規則
- 🎨 修改介面外觀

**盡情發揮創意，打造獨一無二的扭蛋遊戲！** ✨

---

有任何問題歡迎查閱 [完整部署教學.md](./完整部署教學.md) 或 [README.md](./README.md)

