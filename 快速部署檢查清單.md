# ✅ 快速部署檢查清單

按照這個清單逐步完成部署。

---

## 📝 部署前準備

- [ ] 確認專案在本地可正常運行
- [ ] 已安裝 Git
- [ ] 已註冊 GitHub 帳號
- [ ] 已註冊 Render 帳號（後端）
- [ ] 已註冊 Vercel 帳號（前端）

---

## 🔸 步驟 1：推送代碼到 GitHub

```bash
cd D:\TestProject\GachaEgg
git init
git add .
git commit -m "Initial commit"
```

在 GitHub 建立新倉庫後：
```bash
git remote add origin https://github.com/你的用戶名/GachaEgg.git
git branch -M main
git push -u origin main
```

- [ ] 代碼已推送到 GitHub

---

## 🔸 步驟 2：部署後端到 Render

1. 前往 https://render.com/
2. 點擊「New +」→「Web Service」
3. 連接 GitHub 倉庫

**設定：**
```
Name: gacha-game-backend
Root Directory: server
Build Command: npm install
Start Command: npm start
```

**環境變數：**
```
PORT=3000
NODE_ENV=production
CORS_ORIGIN=*
```

4. 點擊「Create Web Service」
5. 等待部署完成

- [ ] 後端已成功部署
- [ ] 後端 URL：`_______________________________`

---

## 🔸 步驟 3：測試後端

訪問：`https://你的後端URL.onrender.com/health`

應該看到：
```json
{"status":"healthy","timestamp":"..."}
```

- [ ] 後端健康檢查通過

---

## 🔸 步驟 4：配置前端環境變數

在本地建立 `client/.env.production` 文件：

```env
VITE_SOCKET_URL=https://你的後端URL.onrender.com
```

提交變更：
```bash
git add client/.env.production
git commit -m "Add production environment"
git push
```

- [ ] 環境變數已配置

---

## 🔸 步驟 5：部署前端到 Vercel

1. 前往 https://vercel.com/
2. 點擊「Add New」→「Project」
3. 導入 GitHub 倉庫

**設定：**
```
Framework Preset: Vite
Root Directory: client
Build Command: npm run build
Output Directory: dist
```

**環境變數：**
```
VITE_SOCKET_URL=https://你的後端URL.onrender.com
```

4. 點擊「Deploy」
5. 等待部署完成

- [ ] 前端已成功部署
- [ ] 前端 URL：`_______________________________`

---

## 🔸 步驟 6：更新 CORS 設定

編輯 `server/index.js`，找到 CORS 設定：

```javascript
app.use(cors({
  origin: process.env.CORS_ORIGIN || '*'
}));
```

在 Render 環境變數中更新：
```
CORS_ORIGIN=https://你的前端URL.vercel.app
```

或保持為 `*`（允許所有來源，測試用）

- [ ] CORS 已更新

---

## 🔸 步驟 7：完整測試

訪問您的前端網址：`https://你的專案名.vercel.app`

測試項目：
- [ ] 頁面正常載入
- [ ] 顯示「✅ 已連線」
- [ ] 可以輸入玩家名稱
- [ ] 單人抽蛋功能正常
- [ ] 可以建立對戰房間
- [ ] 可以加入對戰房間（用另一個瀏覽器測試）
- [ ] 對戰倒數與結果正常
- [ ] 大獎廣播正常顯示

---

## 🎉 完成！

您的遊戲網址：
- 🌐 **前端：** `https://你的專案名.vercel.app`
- 🖥️ **後端：** `https://你的後端名.onrender.com`

---

## 📤 分享給朋友

只需要分享前端網址即可：
```
https://你的專案名.vercel.app
```

---

## 🔄 未來更新流程

1. 在本地修改代碼
2. 測試確認無誤
3. 提交並推送：
   ```bash
   git add .
   git commit -m "描述您的修改"
   git push
   ```
4. Render 和 Vercel 會自動重新部署

---

## ⚠️ 注意事項

### Render 免費方案限制
- 閒置 15 分鐘後會休眠
- 首次訪問需要 30-60 秒喚醒
- 每月 750 小時免費時數

### 解決休眠問題
1. 升級到付費方案（$7/月）
2. 使用 Railway 替代（不會休眠）
3. 使用 [UptimeRobot](https://uptimerobot.com/) 每 5 分鐘 ping 一次

---

## 🆘 遇到問題？

參考完整的「部署指南.md」文件。

